// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch CGPA on Local host",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}